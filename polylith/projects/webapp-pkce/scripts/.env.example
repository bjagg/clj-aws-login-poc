# ------------------------------------------------------------------------------
# Environment configuration for webapp-pkce
#
# Copy this file to `.env` and fill in real values.
# The `.env` file is ignored by git.
#
#   cp .env.example .env
# ------------------------------------------------------------------------------

# ------------------------------------------------------------------------------
# Project identity (must be unique per AWS account + region)
# ------------------------------------------------------------------------------
PROJECT_NAME=aws-login-pkce-demo

# ------------------------------------------------------------------------------
# AWS configuration
# ------------------------------------------------------------------------------
AWS_REGION=us-east-1
AWS_PROFILE=default

# ------------------------------------------------------------------------------
# DNS / domain configuration (Route53)
# ------------------------------------------------------------------------------
# Hosted zone that contains your domain (e.g., rpgsecrets.net)
HOSTED_ZONE_ID=Z1234567890ABCDE

# Full hostname for this deployment (NOT just the base domain).
# Example: sso-poc.rpgsecrets.net
DOMAIN_NAME=sso-poc.example.com

# ------------------------------------------------------------------------------
# Cognito Hosted UI domain prefix (must be globally unique in region)
# ------------------------------------------------------------------------------
COGNITO_DOMAIN_PREFIX=login-pkce-demo

# ------------------------------------------------------------------------------
# Google OAuth (Identity Provider) - stored in AWS Secrets Manager
# ------------------------------------------------------------------------------
# Name of the secret containing:
#   { "clientId": "...apps.googleusercontent.com", "clientSecret": "..." }
GOOGLE_SECRET_NAME=aws-login-google-oauth

# ------------------------------------------------------------------------------
# Local dev redirect/logout (used to build CallbackURLs/LogoutURLs)
# ------------------------------------------------------------------------------
LOCAL_REDIRECT_URI=http://localhost:3000/callback
LOCAL_LOGOUT_URI=http://localhost:3000/
